apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nginx-ingress
  namespace: tesr
spec:
  rules:
  - host: test-argocd.server-in.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: arego
            port:
              number: 80
